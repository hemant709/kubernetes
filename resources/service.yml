apiVersion: v1
kind: Service
metadata:
  name: hemant-service
  namespace: auto-deploy
  labels:
    app: myapp
    component: service
spec:
  selector:
    app: myapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP